<p id="post--categories"><span class="badge badge-secondary">Categories: </span>
    <% if (page.categories && page.categories.length) { %>
        <%- list_categories(page.categories, {
            show_count: false,
            class: "page-category",
            style: "none",
            seperator: ","
        }) %>
    <% } %>
</p>